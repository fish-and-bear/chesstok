{
  "name": "chesstok",
  "version": "0.1.0",
  "description": "A mobile-first chess puzzle feed for quick tactical reps.",
  "type": "module",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "node tools/build-public.mjs",
    "check": "node tools/verify-release.mjs",
    "preview": "python3 -m http.server 8899 --bind 127.0.0.1 --directory .",
    "preview:dist": "npm run build && python3 -m http.server 8899 --bind 127.0.0.1 --directory dist"
  },
  "keywords": [
    "chess",
    "puzzles",
    "tactics",
    "pwa",
    "lichess"
  ]
}
